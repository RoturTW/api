# Example environment configuration for Claw
# Copy to ../.env (root) or adapt for deployment

USERS_FILE_PATH=./users.json
LOCAL_POSTS_PATH=./posts.json
FOLLOWERS_FILE_PATH=./clawusers.json
ITEMS_FILE_PATH=./items.json
KEYS_FILE_PATH=./keys.json
EVENTS_HISTORY_PATH=./events_history.json
DAILY_CLAIMS_FILE_PATH=./rotur_daily.json
SYSTEMS_FILE_PATH=./systems.json

# External services (leave blank if unused)
WEBSOCKET_SERVER_URL=
EVENT_SERVER_URL=
BANNED_WORDS_URL=
DISCORD_WEBHOOK_URL=

# Timing / cache
SUBSCRIPTION_CHECK_INTERVAL=3600
KEY_OWNERSHIP_CACHE_TTL=600

# Admin token (set to a strong random value; never commit real secrets)
ADMIN_TOKEN=
